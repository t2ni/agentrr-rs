# Normalize line endings to LF on checkout, regardless of platform.
# Dev happens on Windows; CI runs on Linux — this keeps them consistent.
* text=auto eol=lf

# Binary artifacts (never normalize).
*.png binary
*.jpg binary
*.ico binary
*.agentrr binary
*.sqlite binary
