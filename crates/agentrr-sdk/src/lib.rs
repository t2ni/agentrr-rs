//! Optional Rust SDK for `agentrr`.
//!
//! For agent loops that don't go through the HTTP proxy: in-process
//! `record`/`replay` guards for `Clock` and `Random`, plus tool-function
//! wrappers so non-deterministic boundaries are captured deterministically.
//! Implemented in M9.
