//! `agentrr` CLI entry point.
//!
//! M0 scaffold — real subcommands (`record`, `replay`, `ls`, `show`, …) land in
//! M1 and beyond.

fn main() {
    println!("agentrr {} (scaffold)", env!("CARGO_PKG_VERSION"));
}
